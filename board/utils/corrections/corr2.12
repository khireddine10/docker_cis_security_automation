#!/bin/bash

printf "\033[1;34m [INFO] 2.12 - Follow this steps to set up authorization plugins:
	Step 1: Install/Create an authorization plugin.
	Step 2: Configure the authorization policy as desired.
	Step 3: Start the docker daemon as : 'dockerd --authorization-plugin=<PLUGIN_ID>' or add authorization-plugins to the daemon configuration. \033[0m\n" 

