#!/bin/bash

str=$(systemctl show -p FragmentPath docker.service | cut -d "=" -f 2)


printf "\033[1;34m [INFO] 1.1.7 - You should try add a rule for the $str in the /etc/audit/audit.rules file, then restart the daemon using 'systemctl restart auditd' \033[0m\n"