#!/bin/bash

printf "\033[1;34m [INFO] 1.1.15 - You should try add a rule for the /usr/bin/containerd-shim in the /etc/audit/audit.rules file, then restart the daemon using 'systemctl restart auditd' \033[0m\n"
