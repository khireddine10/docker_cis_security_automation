#!/bin/bash

echo " ******* 1.1.2 Ensure only trusted users are allowed to control Docker daemon (Automated) *******"
printf "\033[1;34m [INFO] Users: $(getent group docker) \033[0m\n"
printf "\033[1;34m [INFO] You should remove any untrusted users from the docker group using command sudo gpasswd -d <your-user> docker or add trusted users to the docker group using command sudo usermod -aG docker <your-user>. You should not create a mapping of sensitive directories from the host to container volumes. \033[0m\n";